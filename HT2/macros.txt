Imprimir macro registro

 	push ax ; esto es para guardar los valores que traian de antes los registros
 	push dx
 	
 	mov dl,registro
 	add dl,48 ; se le suma 48 al regsitro
 	mov ah, 02h
 	int 21h
 	
 	pop dx
 	pop ax
 	
 endm
 
 Imprimirr8bits macro registro
	 local zero,noz
	 xor ax,ax
	 mov ax,registro
	 mov cx,10
	 mov bx,2
	 
	 zero:
	 xor dx,dx
	 div cx
	 push dx
	 dec bx
	 jnz zero
	 mov bx,2
	 
	 noz:
	 pop dx
	 Imprimir dl
	 dec bx
	 jnz noz
	 
	 
 	
 endm
